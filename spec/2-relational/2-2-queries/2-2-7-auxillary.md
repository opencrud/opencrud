# Queries: Auxillary Types

* Overview
* Node

## Overview

Several auxillary types which are not strongly related to a data type exist. 

## Node

The `Node` interface is implemented by all types in the OpenCRUD schema. 

**Definition**

```graphql
interface Node {
  id: ID!
}
```
